server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false   # Eureka ne s'enregistre pas lui-même
    fetch-registry: false          # il ne cherche pas d'autres services
  server:
    wait-time-in-ms-when-sync-empty: 0
